## Packages
framer-motion | Smooth scroll and entry animations
react-scroll | Smooth scrolling for navigation links
lucide-react | High-quality icons for services and UI
@hookform/resolvers | Form validation with zod

## Notes
- Smooth scrolling is handled via native CSS and Framer Motion.
- Uses `api.contact.create.path` for contact form submissions.
- Tailwind config should extend `fontFamily` with `display` (Outfit) and `body` (Plus Jakarta Sans).
